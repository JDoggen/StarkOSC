import StarkOSC from "../../starkOSC";
import { OSCModule } from "../module";

export class TrackModule extends OSCModule {

    constructor(osc: StarkOSC) {
        super(osc);
    }

    subscribe(): void {
        
    }
}